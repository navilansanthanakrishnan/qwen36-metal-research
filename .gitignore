
.DS_Store
.venv/
*.tmp
# build products from the probes
# local python env for gguf_dump.py only
# transient
# upstream checkout — has its own history, work happens on branches inside it
bench/bwprobe/bwprobe
bench/gpuinfo/gpuinfo
llama.cpp/
wt/

# compiled probes (build with the command in their header comment)
bench/sgmv
bench/sgmap
bench/sgprobe
# asset symlinks: weights and run output live outside the source tree
models
runs

# training venv — tooling, never committed (GOAL: nothing large in the source tree)
.venv-train/

# compiled probes — build them from source, see probe/README.md
probe/mmapeak
probe/scmv
