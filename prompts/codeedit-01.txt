The following Python function has a bug: it returns the wrong result when the
list contains exactly one element, and it mutates its argument. Fix both
problems and print the corrected function in full. Change nothing else.

def running_max(xs):
    """Return a list where element i is max(xs[0..i])."""
    for i in range(1, len(xs)):
        if xs[i] < xs[i - 1]:
            xs[i] = xs[i - 1]
    return xs[1:]
