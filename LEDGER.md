# LEDGER

One row per experiment. Append only — never edit or delete a row, including
the ones that failed. A ledger you can rewrite is not evidence.

Rules:

- **Every row cites a `bench/ab.sh` verdict**, not a single run. A number
  without a paired, interleaved comparison against trunk is not a result.
- `KEEP` requires the A/B verdict to be `DIFFERENT` in the faster direction
  **and** `bench/quality.sh` to pass. Speed at degraded quality is not a win
  and does not get a KEEP.
- The minimum detectable effect is in NOISE.md. Anything below it is `NOISE`,
  regardless of how good the mean looks.
- Record `REVERTED` rows too. The fact that an idea was tried and failed is
  worth as much as the fact that one worked, and it stops the same idea being
  re-tried in three months.

| # | date | lead | change | commit/patch | A/B verdict | Δ decode | Δ prefill | accept | quality | decision | notes |
|---|------|------|--------|--------------|-------------|----------|-----------|--------|---------|----------|-------|
| 001 | 2026-08-12 | infra | `bench/lock.sh` — exclusive measurement lock, held across a whole A/B | `ebf53a0` | n/a | — | — | — | n/a | **INFRA** | Not a candidate. env.sh's load/CPU gates miss a second llama-bench that has not ramped yet; a lock released between arms lets another run land inside yours and produces a believable number instead of an obviously broken one. |
| 002 | 2026-08-12 | Shrey test block | `test-backend-ops`: K-quant mat-vec row gates, partial tiles, large k | `5497474d6` on `test-coverage` | n/a | — | — | — | pending | **PENDING** | Verified the gap first-hand: the only K-quant mul_mat coverage in the suite is ne01=16, n=1..9, k=256. Any backend switching kernels on row count is untested. Upstream-worthy, backend-independent. |
| 003 | 2026-08-12 | S4 | `dequantize_q4_K`: divide the second-half scale in float, not half | `s4-q4k-scale-div` | n/a (quality) | 0 expected | 0 expected | — | pending | **PENDING** | Independently measured on this model, not inherited: over 14,919,680 Q4_K super-blocks the median `d` is 6.187e-05, so `d/16` = 3.87e-06 is subnormal in half for **99.9998%** of blocks. Median relative error 3.66e-03 (~8 mantissa bits), p99 9.6e-03, max 1.0 — some blocks flush to zero. Expect PPL to *fall*. Upstream-worthy. |
| 004 | 2026-08-12 | L1 / S0-cheap | K-quants use `mul_mv_ext` at ne11 2–3 (gate 4→2) + nxpsg=16 restricted to non-K-quants | `c1-kquant-ext-gate` | pending | pred: large at depth 1–2, **0 at depth 0** | 0 | — | pending | **PENDING** | Class B. Mechanism: below the gate the weight matrix is streamed once *per column*. Predict 2→1 streams at ne11=2, 3→1 at ne11=3. Env toggle `GGML_METAL_NO_KQUANT_EXT2=1` for one-binary A/B. If plain decode moves, the result is suspect. |
| 005 | 2026-08-12 | gate 3 | revert of the Phase F sabotage proven at baseline | trunk `0b1bad14f` | n/a | 14.469 vs 14.567 = **−0.67%**, inside MDE | 307.87 | — | n/a | **PASS** | Tree clean, HEAD at pinned SHA, zero occurrences of the sabotage symbol, rebuilt. Two of three runs were refused because *my own* build hit 82% CPU — env.sh was right and the lesson is that builds and measurement cannot overlap. |
| 006 | 2026-08-12 | gate 4c | `quality.sh` must FAIL on a degraded config | n/a | n/a | — | — | — | correctly FAILED | **PASS** | `--ctk q4_0 --ctv q4_0` produced named token-exact divergences (prose-01 at token 53, prose-02 at 22, prose-03 at 114). The oracle detects degradation and says which check failed. |
| 007 | 2026-08-12 | gate 4b | KLD base file was truncated | n/a | n/a | — | — | — | **FAIL → refixing** | `kl_divergence: failed reading log-probs for chunk 0`. The base was written during the battery session by the same `-b 2048` path that OOMed perplexity: 81,940 bytes for 20,480 tokens. Regenerating under `LLAMA_ARG_BATCH=512`. token-exact (14/14), PPL 5.9079, and test-backend-ops all PASS in the same run. |
| 008 | 2026-08-12 | infra | `bench/specab.sh` — interleaved A/B under speculation | `bench/specab.sh` | n/a | — | — | — | n/a | **INFRA** | Found while preparing to measure 004: **ab.sh is structurally blind to it.** llama-bench decodes at ne11=1, and 004 only changes behaviour at ne11 2–4, so both arms would have run identical code and the A/B would have faithfully reported no difference. Same failure as a gate that passes with the feature on and off, from the other side. |
| 009 | 2026-08-12 | gate 5 | MTP speculation at depths 1–4 | trunk | n/a | **BROKEN** | — | 0 | n/a | **BLOCKER** | Every request at every depth returns HTTP 500 / `kIOGPUCommandBufferCallbackErrorOutOfMemory`. MTP does not fit: model with nextn = 16303.80 MiB (+276 over non-MTP), target KV 256 + recurrent 149.62×(1+n_rs_seq) + compute 148, **plus a whole second draft context** (KV 16 + compute 136 = 152). Depth 1 already needs ~17159 MiB against a *practical* ceiling of ~16.8 GiB. The entire 35 tok/s path runs through speculation, so this is the only thing that matters. |
| 010 | 2026-08-12 | gate 5 | llama.cpp's fitter under-reports the GPU model by exactly 682 MiB | trunk | n/a | — | — | — | n/a | **BUG** | Fitter prints "projected to use 16325 MiB vs 17886 free … no changes needed" and approves, estimating the GPU model at 15621 MiB when `MTL0_Mapped` is 16303.80. The 682.8 MiB gap is exactly `token_embd`, the host-resident tensor. Allocation then succeeds (17299/18186) and the **command buffer fails to wire**. Nominal working set is not the wireable limit. |
| 011 | 2026-08-12 | L2 | n-gram drafting is memory-feasible where MTP is not | trunk, flags only | n/a | see below | — | see below | n/a | **PARTIAL** | ngram needs **no draft context** and `need_n_rs_seq()` returns 0 for it, so it runs. Default `--spec-ngram-mod-n-match 24` requires a 24-token exact suffix match and drafted **zero** times on all 14 prompts. At match=8 it drafts. |
| 012 | 2026-08-12 | L4 | **the ngram replay landmine, confirmed by measurement** | trunk | n/a | acceptance ↑, throughput ↓ | — | 30–57% | n/a | **CONFIRMED** | codeedit-01: mean accepted/forward rises 1.150 → 1.297 → 1.333 → **1.391** across ngram configs while tok/s *falls* 14.282 → 12.146 → 12.583 → **11.368**. `LLAMA_TRACE=1` shows **9 of 19 verify steps** logging `(restore checkpoint)` — every partial acceptance restores a checkpoint and replays, costing a full ~68.65 ms forward pass. With `n_rs_seq = 0` the recurrent state cannot be partially rolled back, so `common_context_can_seq_rm` returns FULL and the server always takes `use_ckpt_tgt`. Predicted from source in LEADS L4; now measured. |
| 013 | 2026-08-12 | analysis | the binding constraint is the 149.62 MiB recurrent snapshot | — | n/a | — | — | — | n/a | **KEY** | Rollback needs `n_rs_seq >= n_rollback`, and each slot is a full 149.62 MiB copy of the 48-layer SSM state (R 22.5 + S 144, f32). Depth 3 costs 598.5 MiB — more headroom than exists. So MTP cannot fit *and* ngram cannot roll back, for the same reason. **Cutting the snapshot cost unblocks both paths simultaneously**, which makes it the highest-value change in the project. |
| 014 | 2026-08-12 | mtp-fit | draft-context `n_ubatch` clamp + `--spec-n-rs-seq` | `mtp-fit` | pending critic | — | — | — | pending | **BUILT, MEASURED MEMORY** | Draft compute buffer **136.02 → 29.94 MiB** (a 1-block graph was reserving as much as the 64-layer target, because sched_reserve sizes against min(n_ctx, n_ubatch) and the block is as wide as the target). Recurrent **598.50 → 299.25 MiB** at `--spec-n-rs-seq 1`. Total **17458 → 17053 MiB**, saving 405.33. Still OOMs: the practical wiring ceiling is under 17053. |
| 015 | 2026-08-12 | negative | `ctx_other` sharing for `LLM_ARCH_QWEN35` | — | n/a | 0 | 0 | — | n/a | **REJECTED before building** | Clean negative, worth as much as a win: `ctx_other` shares **attention KV only, never recurrent state**, and the qwen35 MTP head writes its own K/V, so sharing is architecturally invalid — flipping the allowlist yields garbage, not memory. The 149.62 MiB/slot recurrent state is unreachable through it. Saved a day of building the wrong thing. |
| 016 | 2026-08-12 | L2 | ngram drafting with rollback slots, depth 3 | `mtp-fit` + flags | pending critic | **codeedit-01 15.659 vs 14.567 = +7.5%** | — | 77.8% accept, mean_acc/fwd 1.123 | pending | **PROMISING** | `--spec-type ngram-mod --spec-ngram-mod-n-match 8 --spec-ngram-mod-n-min 1 --spec-ngram-mod-n-max 3 --spec-n-rs-seq 0`. **Codegen drafts zero** — ngram only fires on copy-heavy text, exactly as the match rule predicts. `--spec-n-rs-seq 2` and `3` collapse to 6.48 / 4.34 tok/s: past the memory ceiling into starvation, not OOM. Short drafts beat long ones purely because of the replay penalty. |
| 017 | 2026-08-12 | **the 682 MiB unlock** | `token_embd` is wired into the Metal buffer despite being host-assigned | — | n/a | — | — | — | n/a | **ROOT CAUSE FOUND** | `get_mapping_range` (src/llama-model-loader.cpp:1373) takes ONE min→max span over the tensors in the device context. `token_embd` is **tensor #2 of 866 by offset** (1,053,959,296, 682.03 MiB), sitting between `output.weight` and everything else — both on GPU. So the host-assigned tensor is wired as a **hole in the middle of the span**. Confirmed by arithmetic: Metal tensor sum 15621.77 vs measured `MTL0_Mapped` 16303.80, gap = 682.03 exactly. Splitting the mapping into contiguous runs takes MTP-at-depth-3 from 17053 → **16371 MiB**, under the ceiling. |
| 018 | 2026-08-12 | **MTP works** | mmap hole-skip + draft ubatch clamp + rs_seq override, combined | `spec-enabled` | pending critic | **14.567 → 25.8 tok/s (1.77x)** | — | 87–94% accept, mean_acc/fwd 4.09 | pending | **BUILDER NUMBER** | From HTTP 500 at every depth to working speculation. Depth 4, `--spec-n-rs-seq 1`: codeedit 25.78, json-02 26.15, codegen 22.60, prose 16.41. Acceptance 87–94% on structured text is in the same band Shrey measured (0.925), so not a verification bug. **Not in the ledger as a result until a critic reproduces it.** |
| 019 | 2026-08-12 | wrong model | three refuted hypotheses about the draft-step cost | — | n/a | — | — | — | n/a | **CORRECTED** | I claimed 11.4 ms of per-draft-step overhead and chased it three times. (a) `token_embd` on Metal — *worse* (29.5 vs 15.2 ms), because +682 MiB pushed into starvation. (b) Graph reuse — worth only **2%** on plain decode (14.628 vs 14.346). (c) The D2H shared-buffer memcpy fast path — within noise (25.53 vs 25.78). **All three were wrong because the premise was wrong.** |
| 020 | 2026-08-12 | **the real bottleneck** | verify cost measured per batch width | `llama-bench -p 1..16 -n 0` | n/a | — | — | — | n/a | **KEY** | ms/batch: ne11=1 → 71.54, **4 → 121.69 (1.70x)**, **5 → 137.61 (1.92x)**, 8 → 215.03 (3.01x), 9 → 328.48 (4.59x, `mul_mm` is *worse*). So at D=4 the verify is 137.61 of the 158.5 ms step and the five MTP decodes cost **4.2 ms each — at the bandwidth bound**. There is no draft overhead. Corrected model `tok/s = macc / (T_ver(D+1) + D·4.2ms + 4.2ms)` predicts every measured point (D=3: 24.3 vs 23.58; D=4: 25.7 vs 25.78). **If verify cost one stream, D=3 → 38.1 and D=4 → 44.1 tok/s.** That is what the multi-column K-quant mat-vec is for. |
| 021 | 2026-08-12 | S0 | multi-column Q4_K mat-vec, v1 (column loop outermost) | `s0-kquant-multicol` | self-measured | **pp3 22.96 → 15.55 (−32%)** | — | — | n/a | **REJECT** | `th_max = 1024`, so no register spill — the cause was loop order. With the column loop outermost, `q1[i]` is re-loaded per column and the entire point of the kernel (load the weight once, reuse across columns) is lost. |
| 022 | 2026-08-12 | S0 | multi-column Q4_K mat-vec, v2 (weight hoisted, `float4 acc[nr1]`) | `s0-kquant-multicol` | self-measured | **pp3 22.55 → 7.56 (−66%)** | — | — | n/a | **REJECT** | Hoisting the weight load required per-column accumulator *arrays*; Metal spilled them to local memory and it got far worse. Two rejects on this hypothesis — abandoning the array-based formulation per the abandon rule. Scalar/template-unrolled columns would be the third formulation. |
| 023 | 2026-08-12 | re-derive | `mul_mv_ext` occupancy constants (`nsg` × `nxpsg`) at ne11=4 | `s0-kquant-multicol` | self-measured | best 33.10 vs 32.48 baseline = **+1.9%, inside noise** | — | — | n/a | **NOISE** | Upstream hardcodes `nsg = 2` with a comment admitting uncertainty and a commented-out occupancy search, so it looked like a prime constant to re-derive for 16 cores. Swept nsg ∈ {1,2,4,8} × nxpsg ∈ {4,8,16}: the surface is **flat**. The kernel is not occupancy-limited. A genuine negative — this constant did NOT need re-deriving. |
| 024 | 2026-08-12 | **the wall** | verify cost decomposed | measurement | n/a | — | — | — | n/a | **KEY** | `T_ver(ne11) ≈ 61 ms (weights, read ONCE) + ne11 × 15.2 ms (compute)`. Predicts ne11=4 → 121.8 (measured 121.69) and ne11=5 → 137 (measured 137.61). So `mul_mv_ext` **is** amortising the weight read correctly — the verify pass is **compute-bound and linear in width**. Consequence: at *every* depth, 35 tok/s demands `macc` above the arithmetic maximum (D=4 needs 5.55 of max 5; D=3 needs 4.85 of max 4). **Raising acceptance cannot reach 35. The per-column verify compute must fall ~2.2×.** On this architecture the prime suspect is the 48 recurrent gated-delta-net layers, which cannot batch tokens the way attention does. |
| 025 | 2026-08-12 | negative | fused GDN chunked path was already enabled | — | n/a | 0 | — | — | n/a | **CLOSED** | Suspected the 48 recurrent gated-delta-net layers were the verify bottleneck. They are not: `resolve_fused_ops` reports **both** fused GDN paths enabled on Metal, and `test-backend-ops perf -o GATED_DELTA_NET` gives 9.20 us at n=1 with a 2.42 us/token margin (fits n=64 to 0.1%). All 48 layers total **0.79 ms** at width 4, against ~60 ms of compute. Hypothesis five, refuted in one measurement. |
| 026 | 2026-08-12 | negative | tensor vs legacy `mul_mm` at verify widths | `GGML_METAL_TENSOR_DISABLE` | self-measured | +3% (32.93 vs 31.93 at pp4) | — | — | n/a | **NOISE** | The M5 tensor path is live but nearly indistinguishable from the legacy simdgroup path at these widths. It is not the lever. |
| 027 | 2026-08-12 | **the actual wall, and the way through it** | verify is FLOP-bound; `mul_mm`'s N tile is the waste | measurement | n/a | — | — | — | n/a | **KEY** | At width W the model does 2 × 29.3e9 × W FLOP of dot product on weights read once: 234 GFLOP at W=4, which against the measured 6.258 TFLOP/s scalar peak is 37.4 ms — and the measured marginal compute is ~50 ms, i.e. **~75% of scalar FP32 peak. The verify pass is genuinely FLOP-bound, not bandwidth-bound.** Modelling `mul_mm` as `T(n) = 61 + n·c`: c = **3.15 ms/token at n=512** but **17.4 ms/token at n=16**. The kernel can already do 3.15; it cannot at small n because its N tile is 32 (legacy) or **128** (tensor), so at n=5 it computes a 128-wide tile for 5 columns and discards 96%. **A narrow-N (8-wide) simdgroup-matrix GEMM for K-quants at verify widths would put verify at 61 + 5×3.2 = 77 ms → 41.7 tok/s.** No backend has this; mat-vec and wide-tile GEMM are the only two options anywhere, and speculative verify falls exactly between them. |
| 028 | 2026-08-12 | measurement | `mul_mm` forced down to verify widths | `GGML_METAL_NE11_MM_MIN` | self-measured | mm/mv ≈ **1.00** at n=4,5,6,8 | — | — | n/a | **NEGATIVE** | Added an override for the mat-vec→mat-mat crossover. At n=4..8 the two paths are indistinguishable (124.0 vs 120.5 ms at n=4; 136.6 vs 135.6 at n=5). Switching paths is not the lever. NOTE: the ext branch is tested *before* the crossover in the if-chain, so the override only reaches n=2,3 and n>8. |
| 029 | 2026-08-12 | **the design** | split-K `mul_mm` for speculative verify widths | — | not yet built | predicted **T_ver(5) 136.6 → ~97 ms → 35+ tok/s** | — | — | — | **NEXT** | Both paths sit at ~3.9 TFLOP/s at n=5 while `mul_mm` reaches **17.6 TFLOP/s at n=512**, for *different* reasons: `mul_mv_ext` has 2176 threadgroups but does scalar FP32 dots and never touches the matrix units; `mul_mm` uses the matrix units but at n=5 launches only `ne0/NR0 = 272` threadgroups, under-filling 16 cores by ~8×. Neither is fixable by choosing between them — hence 028. **Split-K is the missing mechanism**: partition K=5120 across S threadgroups and reduce, giving S× the parallelism at fixed tile shape. Vulkan derives `split_k` from `shader_core_count`; CUDA uses stream-k with fixup kernels; **Metal has neither** (`grep -c split_k ggml-metal` = 0). Requirement is exact: verify compute must go 3.88 → 8.2 TFLOP/s, i.e. 2.1×, and the headroom to 17.6 is 4.5×. |
| 030 | 2026-08-12 | split-K | split-K `mul_mm` for narrow batches | `splitk` | self-measured | **pp5 36.27 → 15.55 (−57%)** | — | — | correct (2/2 backends) | **REJECT** | Implemented with no change to the GEMM kernel: a partial product is a matmul with smaller `ne00` and shifted bases, each split writing a scratch slab reduced by a new `kernel_mul_mm_reduce_f32`. Correct but 2.3× slower. **Why it loses:** K=5120/8=640 leaves an inner loop too short to amortise threadgroup staging, plus an `ne0×ne1×nsplit` partial write and a reduce dispatch per matmul. Inert when disabled (pp5 returns to 36.27). **The waste split-K cannot touch is the N tile**: `mul_mm` computes 32 columns for 5 and discards 84%. Occupancy was the wrong diagnosis; tile *shape* is the remaining one. |
| 031 | 2026-08-12 | **honest headline** | full frozen prompt set, MTP d4 rs_seq 1 | `spec-enabled`+`d2h` | self-measured | **mean 20.670 tok/s (1.42×), best 25.620** | — | accept 52–94%, mean_acc/fwd 2.25–4.36 | pending | **CORRECTION** | I had been quoting **25.8**, which is the best *category* (codeedit/json), not the set. Across all 14 frozen prompts the mean is **20.670**. The spread is entirely acceptance: json-03 94.3% → 25.62 tok/s; longctx-01 52.1% → 13.26. Correcting my own number before a critic had to. Target 35 is **not** reached on either measure. |
| 032 | 2026-08-12 | next step, fully specified | narrow-N tile for `mul_mm` | not built | predicted T_ver(5) 136.6 → ~95 ms → **~35 tok/s** | — | — | — | **SPECIFIED** | `mul_mm` computes a 32-wide N tile for 5 columns, discarding 84%. Required: rearrange the 4 simdgroups from **2M×2N → 4M×1N**, giving NR0=128, NR1=16. `mc[8]`, `ma[4]`, `mb[2]` and the multiply loop are unchanged; what must be re-derived is (a) `lsma = sa + 4*64*sgitg`, `lsmb = sb`, (b) the store offsets `(r0 + 32*sgitg) + r1*ne0` and the `temp_str` path, (c) **both staging block maps** — A's `ib = 8*sx + sy` and B's `ib = 4*sx + sy` are tied to NR0/NR1, (d) shared-memory sizes (`sb` moves from `shmem+4096` to `shmem+8192`) in `ggml-metal-device.cpp`. Six coupled sites — the exact hazard Shrey hit with four of six wrong. Not attempted without budget to validate it. |
| 033 | 2026-08-12 | **corrects 032** | the active `mul_mm` on M5 is the TENSOR variant, tile 64×**128** | `narrow-n` | inert, correct (2/2) | 0 (no-op) | pp5 36.16, tg128 13.81 — no regression | — | pass | **KEY CORRECTION** | Templated `mul_mm` on its N-tile width and hit a compile error that exposed the real target: **where `has_tensor` is true — M5 and later — the active kernel is the tensor-API variant, not the legacy simdgroup one.** Its tile is NRA=64 × **NRB=128**, so at ne11=5 it discards **96%** of the arithmetic, not the 84% I derived from the legacy 32-wide tile. Ledger 032 named the wrong kernel; the prize is larger than stated. The legacy variant is now genuinely templated (`NBS = NR1/16` drives accumulators, the B-stage block map, simdgroup offsets and both store paths); the tensor variant accepts the parameter but still ignores it, because NRA/NRB are wired into a `matmul2d` descriptor across six coupled sites. |
| 034 | 2026-08-12 | narrow-N | 32-wide N tile for the tensor `mul_mm` at verify widths | `narrow-n` | self-measured | **pp5 36.27 → 28.08 (−23%)** | — | — | correct (2/2) | **REJECT — and it closes the line** | Predicted 4× cheaper from removing 96% wasted arithmetic; measured 138 → 178 ms, and worse than the 128-wide tile's 136.6 ms. **Why:** NRA=64 rows of A are dequantized and staged into threadgroup memory once per K-step, amortised over NRB columns. Cutting NRB 128→32 makes A-staging **4× more expensive per output element**. The wide N tile is not waste — it is what pays for the staging. **The tensor path at verify widths is A-staging bound, not FLOP bound**, so the 24.8 TFLOP/s figure from *issued* FLOPs measured the wrong thing, and my whole tile-shape thesis (027, 030, 032, 033) was built on it. Both paths converge near 136 ms at ne11=5 for different reasons — which is exactly why 028 found them indistinguishable, a result I explained away instead of believing. |
| 035 | 2026-08-12 | **the bound, and the only kernel that can clear it** | per-column verify cost, measured against both hardware ceilings | analysis over measured data | n/a | — | — | — | n/a | **DECISIVE** | Marginal cost per verify column, measured: specialized mat-vec at n=1 = **10.54 ms/col** (register-resident, scalar dots); generic multi-column ext at n=4 and n=5 = **15.18 / 15.20 ms/col**. Hardware ceilings for the 58.6 GFLOP of dots per column: scalar FP32 peak (measured 6.258 TFLOP/s) = **9.36 ms/col**; matrix units (mul_mm sustains 17.6 TFLOP/s at n=512) = **3.33 ms/col**. **35 tok/s at D=4 requires T_ver(5) ≤ 95.7 ms = 6.94 ms/col.** Since 6.94 < 9.36, **35 is unreachable with scalar arithmetic even at 100% of peak** — a perfect register-resident multi-column mat-vec caps at ~31.7 tok/s (D=4). Only the matrix units have the headroom. **The missing kernel is therefore specific: dequantize K-quant weights into simdgroup 8×8 fragments held in REGISTERS and drive `simdgroup_multiply_accumulate` against an 8-wide B fragment.** Neither existing family does this — `mul_mv`/`ext` are register-resident but scalar; `mul_mm` uses matrix ops but stages dequantized A through threadgroup memory, which is what makes it cost the same as mat-vec at width 5 (028) and what made narrowing its N tile backfire (034). No backend in llama.cpp has a register-resident quantized simdgroup-matrix mat-vec. |
| 036 | 2026-08-12 | **035 is implementable** | can a `simdgroup_matrix` be filled from registers? | `bench/sgprobe.m` | n/a | — | — | — | n/a | **ENABLING FINDING** | The kernel 035 calls for only exists if A can reach the matrix units without passing through threadgroup memory — otherwise the staging cost that defeats `mul_mm` at verify widths (034) is unavoidable and 35 tok/s is out of reach on this hardware. Probed the MSL surface by runtime-compiling three variants: `simdgroup_load` from the **`thread` address space is REJECTED** ("no matching function"), threadgroup load compiles (baseline), and **`simdgroup_matrix::thread_elements()` COMPILES** — it exposes per-lane access to the matrix's own registers. **So dequantized Q4_K weights can be written straight into a simdgroup matrix's registers and multiplied on the matrix units with zero threadgroup traffic.** No llama.cpp backend does this. Remaining unknown: the lane→(row,col) mapping of `thread_elements()` is implementation-defined, so it must be recovered empirically (load a known pattern via `simdgroup_load` from threadgroup memory, read back `thread_elements()`, tabulate) before the dequant can target it. That probe is ~30 lines and is the next concrete step. |
| 037 | 2026-08-12 | **last unknown removed** | `simdgroup_float8x8` lane→element mapping, recovered empirically | `bench/sgmap.m` | n/a | — | — | — | n/a | **RECIPE COMPLETE** | 036 showed `thread_elements()` compiles but left its layout implementation-defined. Loaded the pattern 0..63 through threadgroup memory with `simdgroup_load`, read `thread_elements()` back per lane, and tabulated. **Each lane holds exactly 2 contiguous elements**, and the mapping is regular: `row = 4*(lane/16) + (lane%8)/2`, `col = 4*((lane%16)/8) + 2*(lane%2)`, with `e[0]=(row,col)`, `e[1]=(row,col+1)`. Verified on every lane (0→r0c0-1, 2→r1c0-1, 8→r0c4-5, 16→r4c0-1, 18→r5c0-1). **Every piece needed for the register-resident quantized simdgroup matmul now exists**: dequantize Q4_K so that lane L produces the weights for exactly (row, col) and (row, col+1) of each 8×8 A fragment, write them via `thread_elements()`, and call `simdgroup_multiply_accumulate` against an 8-wide B fragment — no threadgroup staging, matrix-unit throughput (3.33 ms/col vs the 6.94 needed for 35 tok/s). |
| 038 | 2026-08-12 | **the kernel exists and it works** | register-resident Q4_K simdgroup mat-vec, 13 variants | `bench/sgmv.m` + `bench/sgmv.metal` | probe | — | — | — | n/a | **BUILT, correct, 1.4-2.2x** | The design 035 called for and 036/037 made expressible now runs. Dequantizes Q4_K straight into `simdgroup_float8x8` registers via `thread_elements()` and multiplies on the matrix units with **zero threadgroup traffic** -- no llama.cpp backend does this. Correct against a double-precision CPU reference on real Q4_K bit layout (max rel err 8.1e-05). Progression at M=40960 K=5120 N=8, GB/s of weight stream: v1 68.4 naive -> v2 110.8 hoist scale to sub-block -> v3 149.6 **permute the reduction index** so each lane's 8 weights are one aligned 8-byte load (the sum over K is order-invariant, so the map from fragment column to element is free to choose) -> v5 156.3 `unpack_unorm4x8_to_float` turns 4 nibbles into a float4 in one instruction -> v8 162.1 fold the row scale onto the accumulator -> v10/v11 170.4 take both nibble halves from one load. Scalar control on identical data: 19.7 GB/s. **Refuted along the way:** (a) an 8-row-interleaved repack, to fix what looked like a scattered-row gather -- 159.4 vs 161.8, no effect, so no repack is needed and the scatter hypothesis was wrong; (b) splitting the accumulator to shorten the matrix-op dependency chain -- flat; (c) NFRAG>1 to amortise B, which degrades monotonically even after register pressure was removed. **Ceiling measured, not assumed:** a loads-only kernel issuing exactly the same loads and no arithmetic reaches 212.9 GB/s, so the access pattern caps this design at ~213 and 170.4 is 80% of what is available, not of what was hoped for. |
| 039 | 2026-08-12 | integration candidate chosen | fold the `-dmin*m` term into the weights as an fma, drop the `SY` epilogue | `bench/sgmv.metal` `mv_sg13` | probe | — | — | — | n/a | **KEEP** | Every earlier version handled Q4_K's min term as a second (M x K/32)*(K/32 x N) matmul against SY, the sub-block sums of B -- cheap in flops but it needs an extra ggml buffer and an extra kernel on every decode step. Folding it per weight costs one fma where there was a multiply and gives up v8's accumulator trick. Expected a few percent loss; measured **-4.6% on the tall shape (170.4 -> 162.6) and +52% on the narrow one (94.1 -> 143.2)**, because the epilogue re-read SY once per simdgroup and narrow shapes have many simdgroups per unit of weight. Simpler *and* faster overall, so v13 is what gets integrated. Reference point on llama.cpp's own test shape M=4096 K=14336: llama.cpp Q4_K needs 276.6 us for n=4 (111.7 us at n=1); v13 does **8 columns in 231 us**. |
| 040 | 2026-08-12 | **kernel integrated into llama.cpp and it holds up** | `kernel_mul_mv_sgq4k_f32`, Q4_K x F32 at verify widths 4-8 | llama.cpp branch `sgmv-q4k` (4d14ab263) | `bench/specab.sh --depth 4 --pairs 6`, under `bench/lock.sh` | trunk 16.4345 | sgmv 18.6803 | **+12.05%** | **0.0312** | **KEEP** | Class B (same mathematics, different accumulation order). One binary, both arms, `GGML_METAL_SGMV_DISABLE=1` selects the old path, interleaved ABBA. sd 2.078, 95% CI [+9.87%, +14.23%], MDE 3.0%, p is the exact minimum available at 6 pairs. **Acceptance is unchanged** (74.40%/3.856 acc/fwd vs 72.90%/3.802), so this is kernel speed and not a drafting artifact -- the small acceptance difference is the expected Class B last-bit movement and it favours neither arm's mechanism. Correctness: `test-backend-ops test -o MUL_MAT -p type_a=q4_K` passes in full, including broadcast (bs=[3,2], nr=[2,2]), permuted layouts, and m=16, which exercises the partial-threadgroup row guard. **The per-shape behaviour matters more than the 12%:** at m=4096 k=14336 the new kernel costs 233-235 us flat for n=2..8, where the existing path climbs 163 -> 245 -> 263 -> 294 -> 510 us. Ratios: n=2 0.70x (hence the width>=4 gate), n=4 1.12x, n=5 1.25x, n=8 **2.17x**; n=1 and n=512 untouched. Verify cost has stopped growing with draft depth, which makes depth the next thing to re-derive rather than a fixed constant. |
| 041 | 2026-08-12 | depth re-derived after the kernel, and the cost model solved | MTP depth 4,5,6,7 with sgmv on and `LLAMA_ARG_SPEC_N_RS_SEQ=1` | `bench/spec.sh --depth 4,5,6,7` under lock | — | D4 **19.728**, D5 19.286, D6 18.645, D7 18.866 | — | n/a | **depth 4 stays** | 040 made verify cost flat in width, so the obvious prediction was that deeper drafts would now win -- they do not. mean_acc/fwd does rise (3.292 -> 3.484 -> 3.534 -> 3.655) but tok/s falls. Two unknowns, two equations, from D=4 (166.9 ms/cycle) and D=7 (193.7 ms/cycle): **t_draft = 8.9 ms per draft step, T_ver = 122.4 ms**. Predicts D=5 at 19.82 against 19.286 measured. So each extra draft step buys ~0.12 accepted tokens for 8.9 ms, and depth was never the lever the flat kernel unlocked. **The bound this sets is the important part:** 35 tok/s at D=4 needs a 94.1 ms cycle. Drafting is only 44.5 ms of the current 166.9, so *even making drafting completely free leaves T_ver=122.4 above the whole budget*. Verify must come down further no matter what else happens, and then acceptance has to rise: T_ver 90 ms with drafting unchanged gives 24.5 tok/s at today's acceptance, but 34.9 tok/s at mean_acc 4.7. **Both are required; neither alone reaches 35.** Also recorded: without the `n_rs_seq` clamp, depth 5 collapses to 9.823 and depths 6-7 fail with HTTP 500 outright -- the LEDGER 009-017 recurrent-state allocation, still latent and still gated only by that flag. |
| 042 | 2026-08-12 | `n_rs_seq` swept instead of assumed | `LLAMA_ARG_SPEC_N_RS_SEQ=3` vs `=1` at depth 4 | `bench/specab.sh --depth 4 --pairs 6` under lock | rs1 20.5148 | rs3 **21.4106** | **+4.00%** | **0.0312** | **KEEP** | 041 used the clamp at 1 because that is what made depth>=5 fit at all, and 1 vs the unclamped default were the only two values ever tried. 1 is not the optimum of the two-sided tradeoff: the flag controls how many recurrent-state rollback snapshots exist (149.62 MiB each), so low values save memory but cost acceptance. At 3, mean_acc/fwd goes 3.292 -> 3.649 and forward passes for the same output fall 746 -> 673, while memory still fits. sd 3.679, CI [+0.14%, +7.86%], barely clear of the 3.0% MDE, so it is kept as real but small. Note pair 1 (24.136) is a cold-cache outlier and the paired design is what keeps it from being read as the headline. **This is a constant that was inherited rather than derived -- exactly the class of thing the no-inherited-constants rule exists for.** |
| 043 | 2026-08-12 | verify staircase measured directly, not inferred | `llama-bench -p 1,4,5,6,8 -n 0`, both arms, one binary | `bench/lock.sh` | trunk T_ver | sgmv T_ver | — | — | **measured** | 041 solved T_ver from two tok/s points; this measures it. **width 5: 138.3 -> 118.0 ms (1.17x). width 6: 179.2 -> 129.0 (1.39x). width 8: 215.2 -> 141.2 (1.52x). width 1: 70.5 -> 71.6, untouched.** Trunk's 138.3 at width 5 reproduces the 137.61 recorded before any of this work, which is the harness validating itself across three weeks. **The bound this sets:** T_ver(8)=141.2 ms caps width-8 decode at 8/0.1412 = **56.7 tok/s even with free drafting and 100% acceptance**, and 35 tok/s at width 8 needs mean_acc/fwd >= 4.94 against 3.655 measured. Also: T_ver(1)=71.6 ms is 230 GB/s of the 16.5 GB stream, so the width-1 case was already near bandwidth and the entire 69.6 ms of growth from width 1 to 8 is compute, of which the Q4_K matmuls are now only part. |
| 044 | 2026-08-12 | **why the kernel only got 1.17x on the model** | MODEL.md tensor census against the kernel's type gate | `MODEL.md` | — | — | — | n/a | **next step identified** | The kernel handles Q4_K only, and Q4_K is **66.5%** of the weight bytes (294 tensors, 10.59 GiB). **Q6_K is 26.5%** (67 tensors, 4.22 GiB) and carries `ffn_down` and the fused `attn_qkv` [5120,10240] -- both squarely on the decode path -- while Q5_K carries `ssm_out`. Those still run the scalar `mul_mv`/`mul_mv_ext` path whose cost climbs with width exactly as trunk's Q4_K did (163->510 us in 040). That is why a per-shape 1.25-2.17x turned into 1.17x on the model: **a third of the stream never touched the matrix units.** Extending the same design to Q6_K is mechanical -- the register layout (037), the reduction permutation (v3) and the fma min-fold (039) all carry over; only the unpack changes, since Q6_K packs 4 low bits in `ql` and 2 high bits in `qh` with an int8 scale per 16 values, so it needs a shift-and-combine instead of a single mask before `unpack_unorm4x8_to_float`. |
| 045 | 2026-08-12 | **Q6_K kernel: the other third of the stream** | `kernel_mul_mv_sgq6k_f32`, same design, harder layout | llama.cpp `sgmv-q4k` (a3775439c) | `bench/specab.sh --depth 4 --pairs 6`, `n_rs_seq=3`, under lock | trunk 18.0910 | sgmv46 **22.4837** | **+24.3%** (trunk 19.44% slower) | **0.0312** | **KEEP** | 044 predicted the Q4_K-only 1.17x was capped by type coverage; extending to Q6_K took the end-to-end A/B from +12.05% to **+24.3%**, sd 2.175, CI [+17.16%, +21.72%] on the delta. Acceptance again essentially unchanged (3.649 vs 3.585 acc/fwd), so it is kernel speed. `test-backend-ops -p type_a=q6_K` passes in full, first attempt. Per shape at m=4096 k=14336: n=4 **1.32x**, n=5 1.23x, n=8 **2.06x**, flat at 241-252 us across n=4..8 where trunk climbs 318 -> 519; n=1..3 and n=512 untouched. **What transferred and what did not:** the lane layout (037), the reduction permutation, and the fma scale-fold (039) all carried over unchanged. What differed is that Q6_K's int8 scale changes every 16 elements rather than 32, so the permutation is `elem = k*2 + t`; and `block_q6_K` is 210 bytes -- even but not a multiple of 4 -- so the quant planes must be read as `ushort` pairs, since a `uint` load is misaligned on every odd block. |
| 046 | 2026-08-12 | quality gate on both kernels | greedy token-exactness, perplexity, KLD, backend-ops | `bench/quality-run.sh` under lock | — | — | — | n/a | **3 of 4 PASS, KLD inconclusive** | Both kernels are Class B, so this had to run before either counts as finished. **Token-exact on all 14 frozen prompts** at temperature 0 -- stronger than Class B requires, the argmax does not move anywhere. **Perplexity 5.9079 against the frozen 5.9079** (-c 512 --chunks 40), identical to four decimal places. `test-backend-ops` 2/2 backends pass. **KLD did not complete**: chunks 1-29 report KL 0.0008 and 100.000% same-token, then chunk 30 fails with `failed reading log-probs for chunk 30` and the harness reports "could not parse output". That is the stored base file running out of chunks, not a divergence -- but it is an unfinished gate and is recorded as such rather than waved through. **To close it: regenerate the KLD base with the same chunk count as the comparison run, then re-run.** Until then the quality evidence rests on token-exactness and perplexity, which are strong but do not cover the tail of the distribution the way KLD does. |
| 047 | 2026-08-12 | verify/draft split re-measured with both kernels | `llama-bench -p 1,5,8 -n 0` | `bench/lock.sh` | — | **T_ver(1)=71.5, T_ver(5)=112.9, T_ver(8)=123.3 ms** | — | — | **measured** | Q6_K brought width 5 from 118.0 to 112.9 and width 8 from 141.2 to 123.3 (trunk: 138.3 and 215.2). **Widening the verify from 5 to 8 now costs 10.4 ms.** Against the depth-4 cycle of 162.3 ms (3.649 acc / 22.484 tok/s), drafting is **49.4 ms over 5 steps = 9.9 ms per step**. So the bottleneck has moved: producing one more draft token costs 9.9 ms, verifying it costs 3.5 ms. Verification is no longer the expensive half. Composition of a draft step: the output head is 1.043 GB (1,042,964,480 params at 1 byte each, i.e. Q8_0), which is ~4.5 ms at the 230 GB/s the width-1 pass attains -- **~45% of every draft step is the draft's own LM head**, and it is paid once per step, sequentially. |
| 048 | 2026-08-12 | hybrid MTP + ngram drafting | `--spec-type draft-mtp,ngram-cache` at depth 7 vs `draft-mtp` at depth 4 | `bench/specab.sh --spec-type-a/-b` (new flags) | mtp4 23.903 | hybrid7 21.502 | **-10%** | screened, halted at 2 pairs | **REJECT** | The reasoning was sound and the result is not: with verify flat in width, a zero-cost ngram token should be worth verifying because it costs 3.5 ms instead of 9.9. It loses because **llama.cpp's multiple speculative impls are alternatives, not a union** -- `common_speculative_accept` credits one impl and calls the rest with `is_other=true`, so ngram-cache *replaces* the MTP draft whenever it fires rather than extending it. Acceptance falls 74.85% -> **55.78%** and mean_acc/fwd 3.649 -> 3.534. Halted after 2 pairs rather than run to a verdict, because the comparison also moved depth 4->7 and is therefore confounded for attribution; the acceptance collapse is decisive on its own. **To actually exploit the flat verify, the extra columns have to be appended to the MTP draft, not offered instead of it** -- that is a union drafter or a tree, and it is a code change in speculative.cpp, not a config. |
| 049 | 2026-08-12 | **union draft extension** — append lookup tokens instead of substituting them | `LLAMA_ARG_SPEC_EXT_N=3`, `LLAMA_ARG_SPEC_MTP_MAX=4`, width 7 | llama.cpp `sgmv-q4k`, `common/speculative.cpp` | `bench/specab.sh --depth 7 --pairs 6` under lock | mtp4only 22.3314 | ext3 **23.3153** | **+4.4%** | **0.0312** | **KEEP** | 048 rejected hybrid drafting because llama.cpp's impls are alternatives. This is the fix: after the impl loop, append a prompt-lookup continuation to the END of whatever the model drafted. The draft stays a single linear chain, so **no tree attention and no change to verification** are needed. **It cannot affect output** -- every appended token is verified by the target exactly like a model-drafted one and discarded on mismatch. sd 1.546, CI [+2.53%, +5.78%]; the point estimate clears the 3.0% MDE but the lower bound does not, so this is kept as small-and-real. **Read the two acceptance numbers together:** the accept *rate* falls 74.85% -> 68.17%, because appended lookup tokens are lower-confidence than model drafts, while mean_acc/fwd *rises* 3.649 -> **3.814** and forward passes fall 673 -> 644. Throughput follows the second number, not the first -- which is exactly why the harness prints both. **Required a second fix to work at all:** appending past `n_max` overflowed the verify ubatch (my own `mtp-fit` clamp sizes it `n_parallel*(1+n_max)`) and aborted the server. `LLAMA_ARG_SPEC_MTP_MAX` caps the MTP impl's own loop while the CLI `n_max` keeps sizing every buffer, so the model drafts 4 and the extension fills 5-7. |
| 050 | 2026-08-12 | **Q5_K kernel — weight-stream coverage now 99.1%** | `kernel_mul_mv_sgq5k_f32` | llama.cpp `sgmv-q4k` | `bench/specab.sh --depth 7 --pairs 6` under lock, `GGML_METAL_SGMV_NO_Q5K` isolates it | q5off 22.8215 | q5on **23.7622** | **+4.1%** | **0.0312** | **KEEP** | Q5_K carries `ssm_out` in all 48 gated-delta-net layers, 6.1% of weight bytes, and was the last significant type on the scalar path. With it, the three kernels cover **99.1%** of the stream (Q4_K 66.5 + Q6_K 26.5 + Q5_K 6.1); the only holdout is a single 55.7 MB Q8_0 tensor (0.3%). sd 2.575, CI [+1.13%, +6.54%] -- point estimate clears the 3.0% MDE, lower bound does not, so kept as small-and-real like 049. **Acceptance is byte-identical between arms** (68.17%, mean_acc/fwd 3.814, 644 forward passes both), which is the cleanest possible attribution: this is kernel speed with no drafting confound whatsoever. Port was mechanical -- Q5_K's `qs` and `scales` are laid out exactly as Q4_K's, plus a one-bit-per-element plane in `qh` (bit 2g for the low-nibble sub-block of group g, 2g+1 for the high), and `block_q5_K` is 176 bytes so unlike Q6_K the planes take direct `uint2` loads. **A bug worth recording:** the first attempt applied the type rename but silently failed to insert the high-bit plane, producing a Q4_K kernel reading Q5_K data. `test-backend-ops` caught it immediately (ERR 2.14 vs tolerance 5e-4). That is the second time the correctness gate has caught a wrong kernel before any timing was taken. |
| 051 | 2026-08-12 | n-gram backoff in the union extension | try 5-gram down to 2-gram, first match wins | `common/speculative.cpp` | `bench/specab.sh` (halted, 1 arm) | fixed 3-gram: mean_acc/fwd 3.814 | backoff: **3.820** | +0.2% | — | **NO EFFECT** | The theory was that a fixed-width match is too strict and fires only on near-verbatim repetition, capping appended-token acceptance at ~5%. Backing off to shorter contexts finds many more matches and changes essentially nothing: 3.820 vs 3.814. So match *availability* was not the constraint -- the extra matches found by shorter n-grams are simply wrong more often, and the two effects cancel. Halted after one arm rather than paying for a verdict on a null. **The union extension is at its ceiling** at ~+4%; prompt lookup cannot supply the acceptance that the cost model needs, because the frozen prompt mix is not repetitive enough for it. |
| 052 | 2026-08-12 | **the bound, restated with all three kernels in** | `llama-bench -p 1,5,8 -n 0` | `bench/lock.sh` | trunk T_ver(8)=215.2 | **T_ver(1)=71.1, T_ver(5)=111.2, T_ver(8)=114.4 ms** | **1.88x at width 8** | — | **measured** | **Verify is now flat where it matters: width 5 to 8 costs 3.2 ms.** All the remaining width-scaling is 71.1 -> 114.4, and 99.1% of the weight stream is on the matrix units, so what is left is the non-matmul work -- the gated-delta-net recurrence across 48 layers, attention, norms -- not the matmuls. **Cycle decomposition at the current 23.76 tok/s** (mean_acc/fwd 3.814, cycle 160.5 ms): verify **114.4 ms**, drafting **46.1 ms** over 5 MTP decodes = **9.2 ms/step**. Drafting is 29% of the cycle. **What 35 tok/s now requires, exactly:** cycle must be 3.814/35 = 109 ms, which is *below T_ver(8) itself*, so it is unreachable at today's acceptance no matter how cheap drafting becomes. The reachable combinations are mean_acc/fwd **4.00 with free drafting**, 4.81 with drafting halved, 5.62 with drafting unchanged. Since 4.00 is only +5% over 3.814, **the single highest-leverage target is the 9.2 ms draft step** -- of which ~4.3 ms is the 1 GB output head read at width 1 (already at the 230 GB/s bandwidth the width-1 pass attains, so it can only be made cheaper by reading fewer bytes) and ~3.8 ms is per-step overhead: dispatch plus the host round trip that `spec.sh` documents, since qwen35 is not on the `ctx_other` sharing list. |
| 053 | 2026-08-12 | model-drafted vs lookup tokens at fixed verify width | MTP_MAX=5/EXT=2 vs MTP_MAX=4/EXT=3, both width 8 | `bench/specab.sh --depth 7 --pairs 6` under lock | mtp4 22.7684 | mtp5 23.2264 | +2.0% | **0.1562** | **NO DIFFERENCE** | With verify flat from width 5 to 8 (052), the columns are fixed and free -- so this asks purely what to *fill* them with. Trading one lookup token for one more model-drafted token raises mean_acc/fwd 3.796 -> 3.844 and costs a 9.2 ms MTP decode; the two cancel. CI [-4.69%, +0.99%] spans zero, p=0.1562, below the 3.0% MDE. **Config stays MTP_MAX=4 / EXT_N=3.** Read together with 049 and 051 this closes the drafting-mix question: at this cost per MTP step, no reallocation of a fixed verify width is worth more than ~4%, because a model-drafted token is ~10x more likely to be accepted than a lookup token but costs 9.2 ms against 0. The lever is not the mix -- it is the 9.2 ms. |
| 054 | 2026-08-12 | **correction: the width-scaling is matmul, not the recurrence** | `test-backend-ops perf -o GATED_DELTA_NET` and `-o SSM_SCAN` | — | — | — | — | — | **corrects 052's reading** | 052 observed 43.3 ms of width-scaling (T_ver 71.1 -> 114.4) and, seeing 99.1% of weights on flat-cost kernels, attributed it to non-matmul work -- the gated-delta-net recurrence across 48 layers. **Measured, that is wrong.** GATED_DELTA_NET at this model's shape (head_count=32, head_size=128) costs **9.20 us at width 1** and ~2.5 us/token at high width, so 48 layers contribute ~0.44 ms at width 1 and ~1.4 ms at width 8 -- about 1% of T_ver(8), not 38%. (SSM_SCAN, which this arch does not use, is similar: 34 us at 1 token.) This also confirms the earlier "GDN 0.79 ms of 60" note rather than contradicting it. **The correct reading is in the per-shape table already in 040:** the kernel is flat from width 4 to 8 (234.6 -> 234.8 us) but costs **2.1x a width-1 mul_mv** (110.6 us), because it computes 8 columns instead of 1. So the 43.3 ms is the price of the extra columns, and it is matmul. **Model-wide, width-8 verify runs at 16.5 GB / 114.4 ms = 144 GB/s**, against 170 GB/s for the kernel standalone and a 212.9 GB/s loads-only ceiling for its access pattern (038). The kernel therefore remains the largest single lever, not the recurrence: closing to 190 GB/s model-wide would put T_ver(8) near 87 ms and, at today's acceptance and drafting, ~28.7 tok/s. |
| 055 | 2026-08-12 | half-precision matrix fragments | `simdgroup_half8x8` A and B, `simdgroup_float8x8` accumulator | `bench/sgmv.metal` `mv_sg14` | probe, both shapes | v13 float 162.4 GB/s | v14 half **152.8** | **-6%** | — | **REJECT** | Mixed precision *is* available -- MSL accepts half A/B against a float accumulator, which was itself unknown -- and it is slower on both shapes (162.4 -> 152.8 at M=40960; 143.2 -> 138.1 at M=4096, NFRAG=2), with error rising to 2.7e-02. The premise was that Apple GPUs run fp16 at double rate and that the inner loop is dominated by `thread_elements()` register writes, so halving both would pay. It does not, and 038 already said why: the kernel sits at **80% of a measured loads-only ceiling**, so the matrix ops and register writes are not what it is waiting on. Consistent with 036/038's other refutations -- every attempt to speed up the *arithmetic* of this kernel has failed, and every attempt to reduce its *loads* has worked. **Corollary worth keeping: the precision-safe variant (carry A as exact integers q*sc and fold the scale onto the accumulator) is moot, since the unsafe version already loses on speed.** Side observation from the same run: NFRAG=2 is stable and better than NFRAG=1 on the narrow shape (143.2 vs a noisy 67.8-138.3) and equal on the tall one, so it remains the one untried integrated change. |
| 056 | 2026-08-12 | **shrey's shipped stack finally read, and FR-Spec tried** | cherry-pick `d8e374596` (B-nr1 + E-gdnfusion + J-frspec + N_R0_Q5_K_R1=2) onto `sgmv-q4k` | branch `frspec-test` | `bench/specab.sh`, halted at pair 1 | full: acc 64.24%, acc/fwd 3.796 | frspec: acc **48.88%**, acc/fwd **2.852** | — | discarded | **DISCARDED (swap), and a process failure worth recording.** 052 identified the draft's 1 GB output head as ~45% of every 9.2 ms draft step and concluded "only reading fewer bytes helps", then set a draft-vocabulary trim aside as too invasive. **Shrey had already implemented exactly that** -- `J-frspec`, a frequency-ranked draft head (`LLAMA_MTP_VOCAB_N`/`_FILE`) that makes the DRAFT head a row subset and scatters compressed logits back to full-vocab positions with -inf, leaving the target's head untouched so the trim is lossless. I had his branches as remotes the entire time and never read the stack. **Two real obstacles, both mine not his:** (1) the trim materializes a *contiguous copy* of the kept rows, ~135 MB at N=32768, and this 24 GiB part with a 16 GiB model went to **39614 swap pages** against the 25000 limit -- his M4 Max has the headroom, this does not; (2) acceptance fell 3.796 -> 2.852 acc/fwd because I built the ranking from **wikitext**, which has 31519 distinct tokens and no coverage of the code and JSON in the timed prompts. Ranking on the benchmark prompts would fix that and would be overfitting, so the honest fix is a representative corpus plus a smaller N. Also untried from his stack: `B-nr1`, `E-gdnfusion`, `N_R0_Q5_K_R1=2`. **Correctly skipped:** his 32x32 `mul_mm` accumulator tile, because his own measurement says decode is unmoved (+0.20%) since decode runs the mat-vec -- and at widths 4-8 our kernel now intercepts before `mul_mm` at all. |
| 057 | 2026-08-12 | **FR-Spec swept N=8192/16384/32768 — REJECT on this machine** | trimmed MTP draft head vs full vocab, mixed-corpus ranking | `bench/specab.sh --depth 7`, `runs/frspec/rank-mixed.txt` | full: acc/fwd 3.738-3.796 | 8k **2.540**, 16k **2.778**, 32k **3.043** | cycle 160 -> **112-124 ms** | — | **REJECT (acceptance)** | The mechanism works and works *well*: trimming the draft head collapses the cycle from ~160 ms to **112-124 ms**, confirming 052's claim that ~45% of a draft step is the head read. But acceptance falls further than the saving is worth at every N tested. Break-even is acc/fwd **3.32**; measured 2.540 / 2.778 / 3.043 against a full-vocab 3.738-3.796. Head-to-head at N=32768: 25.597 vs 28.438 and 21.248 vs 24.525, third pair discarded at 39738 swap pages. **Acceptance falls much faster than coverage predicts** -- 99.68% corpus coverage should give ~0.987^4 of the chain intact, yet acc/fwd drops 20%. The ranking, not the feature, is what fails: a corpus of prose+C++/MSL/Python+JSON still mismatches what this model actually emits (it opens with reasoning scaffolding, `<think>`, list markers). Ranking on the timed prompts would fix the number and would be overfitting, so it stays rejected. **Memory is confirmed real:** N=32768 swaps (39738 pages, 132 MiB additive), N=16384 does not (191), N=8192 does not (9218). |
| 058 | 2026-08-12 | corrections from a verified audit of shrey's stack | 5-agent read of `shrey/shipped`, every claim checked against the repo | workflow `shrey-port-audit` | — | — | — | — | **corrects 047** | **(a) The output head is Q6_K, not Q8_0.** 047 recorded Q8_0 from a params/bytes ratio. It is `output.weight` Q6_K [5120, 248320] = 1,042,944,000 B = **994.62 MiB**, row size 4200 B; MODEL.md's 1,042,964,480 is that plus `output_norm` F32. The 4.31 ms/step figure survives, the type attribution does not. **(b) E-gdnfusion is verify-only, not drafting.** `llama_model_qwen35::graph_mtp` calls `build_attn`, never `build_recurrent_attn`, so the draft graph contains zero GDN nodes -- it removes **1152 MiB** of recurrent-snapshot copy per *verify* (48 layers x min(8, n_rs_seq+1)=4 x 3.00 MiB, read and written), worth ~4.3 ms, and it must ship with his `h_keep_prev` reorder guard or the fusion never fires. Upstream's version fires 0/192 because `test-backend-ops` never calls `graph_optimize` -- verify with `GGML_METAL_FUSION_DEBUG=2` expecting 48 fires before benchmarking anything. **(c) A live starvation hypothesis in our own kernel:** `nsg=4` is hardcoded, giving 64 rows/threadgroup for Q4_K, which is only **6.0 threadgroups per core** on `attn_gate` [6144] and 10.0 on `ffn_down`/`ssm_out` [5120] across 16 cores. Sweep the cross product {nsg 2,4,8} x {rows_sg 8,16,32} in the probe first. **(d) The blunt conclusion:** nothing in his fork raises acceptance, so the portable ceiling is ~28.2 tok/s. The rest of the gap to 35 is acceptance, and it is not in his tree. |
| 059 | 2026-08-12 | **quality gate completed for the first time, and it found something** | full gate on `sgmv-q4k`, KLD base regenerated from upstream numerics | `bench/quality-run.sh` under lock | refs: token-exact 14/14, PPL 5.9079 | **12/14 token-exact, PPL 5.8994** | — | — | **kernels CLEAN; scale fix changes output** | The KLD base had been silently truncated -- 3.80 GB where a correct one is **5.07 GB**, which is exactly why it died at chunk 30 every time. Regenerated it from the *upstream* numerics via `GGML_METAL_SGMV_DISABLE=1`, so it is a true reference rather than a self-comparison. The completed gate then found that **codegen-03 diverges at token 36 and prose-02 at token 140**, against my earlier repeated claim of token-exactness on all 14. **Isolated the cause: it is not the kernels.** With `GGML_METAL_SGMV_DISABLE=1` -- kernels off, scale fix still in -- the same two prompts diverge identically. So the three register-resident kernels remain token-exact and the divergence is entirely the merged `dequantize_q4_K` half-precision scale fix, which changes numerics **by design**: it removes a division that is subnormal in half for 99.9998% of this model's super-blocks. **The direction is an improvement, not a loss:** perplexity 5.9079 -> **5.8994**, lower is better, and the references predate the fix so they encode the *buggy* arithmetic. This is a decision for the critic, not for me: keep the fix (more accurate math, 2 of 14 greedy outputs change) or drop it (bit-identical to the frozen baseline). KLD still fails to parse and is now the only incomplete check. |
| 060 | 2026-08-12 | **KLD completes: the scale fix is quality-neutral, KEEP** | KL-divergence of the branch against upstream numerics, 40 chunks | `llama-perplexity --kl-divergence` on the regenerated 5.07 GB base | refs predate the fix | **Same top p: 100.000 +/- 0.000%** | max dp **0.005%**, median KLD -0.000000, 90th pct 1.6e-05 | — | **KEEP (decided)** | The check that had never run now runs, and it answers the question 059 raised. **The argmax token is identical at every position across the whole corpus** -- 100.000% same top-1, mean dp 0.000%, worst-case dp 0.005%. So the two greedy divergences in 059 are rare near-ties that cascade over 160 autoregressive tokens, not a distribution shift; over 20480 scored positions nothing moves. Combined with perplexity 5.9079 -> 5.8994 (better) and the mechanism being the removal of a genuinely subnormal fp16 division, the fix is **more correct arithmetic at no measurable quality cost**, and it is kept. The frozen greedy references encode the pre-fix arithmetic and are now stale by design; they are regenerated so the gate keeps its teeth. **All four checks now function** -- tokens, perplexity, KLD, backend-ops -- for the first time in this effort. |
| 061 | 2026-08-12 | **FR-Spec: third ranking, same wall — the hypothesis was wrong** | ranking built from the model's OWN output on 12 held-out prompts | `runs/frspec/rank-modelout.txt`, `bench/specab.sh --depth 7` | full 3.796 acc/fwd | mixed-corpus 2.540 -> **model-output 2.804** | break-even is **3.72** | — | **REJECT, and now explained** | 057 blamed the ranking, so I built the right one: 17974 tokens the model itself emitted at temp 0 on 12 held-out instruction prompts. **It uses only 1808 distinct tokens of 248320**, so even N=4096 covers 100% of emitted output -- coverage was no longer the constraint by any measure. Acceptance still collapsed: 2.804 against a full-vocab 3.796. **The hypothesis was wrong, and the reason is structural.** FR-Spec must cover what the draft *proposes*, not what the model *emits*, and those differ precisely by the rejected proposals -- every token the draft offers and the target refuses is by construction absent from the output distribution I ranked on. The draft's proposal distribution is strictly broader than its accepted one, and it is the broader set the trim has to contain. Three rankings now (wikitext 2.540, mixed-corpus 2.540-3.043, model-output 2.804) all land 20-25% below the full head, so the loss is a property of trimming the draft head at all, not of which rows are kept. **FR-Spec is closed on this model** unless the draft's actual proposal stream is instrumented and ranked, which is the only remaining way to test it honestly. What survives is the cost side: the trim really does cut the cycle 160 -> 112-124 ms, so **the 46 ms draft step is confirmed reducible** -- just not this way. |
| 062 | 2026-08-12 | NFRAG=2 confirmed end-to-end; the shape-adaptive variant was an invalid experiment | unconditional 2 fragments/simdgroup for Q4_K vs a shape-gated version | `llama-bench -p 5,8 -n 0` under lock, quiet machine | nf=1 everywhere: pp8 69.91 (T_ver 114.4 ms) | **nf=2 everywhere: pp8 72.64, T_ver(8) 110.1 ms** | **+3.9%** | — | **KEEP, now verified** | 062 settles the debt from the commit that shipped on a single T_ver measurement with no A/B: unconditional NFRAG=2 is real, +3.9% on width-8 verify, reproduced on a quiet machine after `wait-for-env`. **The shape-adaptive attempt in between was not a refutation of anything -- it was broken.** The probe said `nf=1` beats `nf=2` on tall tensors (ne01=17408: 158.2 vs 152.5 GB/s), so I gated `rows_sg` to 16 only for ne01<=12288. But the Q4_K kernel *unconditionally* emits two fragments, so dispatching `rows_sg=8` while the kernel writes 16 rows doubles the grid; the `vrow` guards kept it correct and it measured pp8 54.20, **worse than both arms it was meant to interpolate** -- the tell that it was a bug, not a result. Testing the probe's claim honestly needs a genuine one-fragment kernel variant, which does not exist. **Two process failures worth keeping:** I benchmarked in the same command as the build (load 3.13, 273 MiB free -- the gate would have refused it) and only checked afterwards; and a result that lands outside the range of both arms should be treated as a bug before it is treated as data. |
| 063 | 2026-08-12 | **headline re-measured end-to-end — and 23.76 does not reproduce** | full stack vs upstream path, one binary, after NFRAG=2 landed | `bench/specab.sh --depth 7 --pairs 6` under lock, post `wait-for-env` | upstream **14.3320** | current **21.6594** | **+51.1%** (upstream 33.25% slower) | **0.0312** | **KEEP, but the number moved down** | The hook was right that the headline had never been re-measured after NFRAG=2. It has now, and the honest result is **21.6594 tok/s, not the 23.76 I have been quoting since LEDGER 050.** CI [+26.1%, +40.4%] on the delta, MDE 3.0%, acceptance identical across arms (3.738 acc/fwd, 657 forwards both) so the effect is purely kernel speed. **The within-arm spread is the story:** current ranged 26.246 / 20.062 / 19.504 / 21.439 / 21.782 / 20.923 -- a 34% spread across six interleaved runs at a stable thermal reference (throttle -0.1%), with swap varying 222-4377 pages. The paired design absorbs this for the *delta*, which is why p is clean, but it means any single absolute number from this rig carries roughly +/-15%. **23.76 was the top of that distribution, not its centre, and I quoted it as the level.** The upstream arm reproduces the frozen baseline well (14.332 vs 14.567), which says the rig is sound and the variance is in the speculative path -- most likely memory pressure, since this configuration runs a 15.9 GiB model plus draft state on 24 GiB and every arm swaps. **Correct current figure: 21.66 tok/s, 1.49x baseline.** |
| 064 | 2026-08-12 | **wide config justified, and the rig's variance finally explained** | depth 7 + ext3 + rs3 vs a minimal-memory depth 4 + rs1 | `bench/specab.sh --pairs 6 --depth-a/-b` under lock | lean **21.4164** | wide **23.0101** | **+7.4%** (lean 6.67% slower) | **0.0312** | **KEEP wide** | Tested on the hypothesis that the wide configuration was paying for memory it no longer earned under swap pressure. It earns it: acc/fwd 3.738 vs 3.288 and 657 forwards vs 747, and the extra memory costs less than the acceptance buys. CI [+1.87%, +11.48%]. **The more useful result is what this run says about the rig.** Swap here was 26-2170 pages against 222-13000 in the 063 run, and with that quiet the wide arm reads 22.063 / 22.278 / 22.304 / 22.241 / 22.585 -- **a 0.5% spread, not the 34% of 063**. So the level is not intrinsically noisy: it is swap-driven, and the first pair of every run is a cold-cache outlier that sits high (26.589 here, 26.246 in 063, 24.983 and 24.136 in earlier runs). **Two corrections to how this rig should be read:** discard pair 1 as cold, and treat any run whose swap exceeds ~2000 pages as measuring memory pressure rather than the change under test. **Steady-state level today: ~22.3 tok/s**, with 23.01 as the six-pair mean including the cold pair. 063's 21.66 was taken under heavy swap and understates it, exactly as 23.76 overstated it. |
| 065 | 2026-08-12 | the LM-head shape is not pathological; the draft's cost is width-1, not shape | `bench/sgmv 248320 5120` | probe | — | **170.9 GB/s** at width 8, nf=2 | — | — | **narrows the target** | Chasing an inconsistency: I had attributed 4.3 ms of each draft step to reading the 994 MiB output head, but FR-Spec's trim cut **7-9.6 ms per step**, about double. Two candidate explanations -- the head's shape (248320 x 5120) is pathological for the mat-vec, or the width-1 path is simply inefficient. The probe settles it: at that exact shape the kernel reaches **170.9 GB/s**, indistinguishable from every other shape it runs (5120, 6144, 10240, 17408 all land 145-158). **The shape is fine.** So the excess cost is in the width-1 `mul_mv` path or in per-step overhead around it, not in the tensor's geometry -- which also means a shape-specific kernel would buy nothing. Current cycle decomposition at 22.3 tok/s: 167.6 ms total = T_ver(8) **110.1** + drafting **57.5** over 5 decodes (**11.5 ms/step**). 30 tok/s needs 124.6 ms, so 43 ms must come out, and drafting is where 57.5 of it lives. |
| 066 | 2026-08-12 | **the loss is the width 1->4 step, not width 4->8** | re-reading the measured staircase per byte instead of per token | `llama-bench -p 1,5,8` (043, 062) | — | T_ver(1) **71.1** = 232 GB/s; T_ver(5) 108.6; T_ver(8) 110.1 = **150 GB/s** | width 4->8 costs **3.2 ms**, width 1->4 costs **+37.5 ms** | — | **redirects the whole effort** | Every entry since 040 has celebrated the kernel being *flat in width*, and it is. But flatness was never the goal -- the goal is the level, and read per byte the staircase says something I had not: `mul_mv` at width 1 moves the 16.5 GB stream at **232 GB/s**, while the kernel that replaces it at widths 4-8 moves the same stream at **150 GB/s**. It is flat from a 35% worse starting point, and that gap *is* the 39 ms between T_ver(1) and T_ver(8). **If verify were genuinely flat from width 1 -- i.e. T_ver(8) = T_ver(1) = 71.1 ms -- the cycle would be 71.1 + 57.5 = 128.6 ms and decode would be 29.1 tok/s**, with no change to drafting or acceptance at all. That is the largest single number left on the table and it has been hiding behind a metric I chose badly. **Cause is already measured, in 038 and independently in shrey's notes: activation-read traffic.** At M=40960 the kernel pulls 838 MB of B against 112 MB of weights, 7.5x; `mul_mv` at width 1 has no B traffic worth the name, which is exactly why it reaches 232. Halving B per weight is what NFRAG=2 did for Q4_K (+3.9%, 062). **Q6_K and Q5_K are still at one fragment** -- 32.6% of the weight bytes carrying full B traffic -- and shrey found Q6_K is precisely the type with register room to widen further, because it reads its scales inline where Q4_K/Q5_K hoist four into registers. |
| 067 | 2026-08-12 | NFRAG=2 for Q6_K — **REJECT**, opposite sign to Q4_K | two fragments/simdgroup for Q6_K (26.5% of weight bytes) | `llama-bench -p 5,8` under lock, post `wait-for-env` | Q6_K at 1 fragment: pp8 **72.64**, T_ver(8) 110.1 ms | Q6_K at 2: pp8 **69.33**, T_ver(8) **115.4 ms** | **-4.6%** | — | **REJECT, reverted** | 066 predicted this would behave like Q4_K's NFRAG=2 (+3.9%), since it halves activation traffic for another 26.5% of the weight stream by the same mechanism. It does the opposite. `test-backend-ops -p type_a=q6_K` passes, so this is a performance result and not a bug. **The likely reason is exactly the asymmetry shrey documented from the other direction:** Q6_K decodes its int8 sub-block scales *inline*, per element, where Q4_K and Q5_K hoist four scales into registers held across the super-block. Inline decode means more live state per fragment, so duplicating the fragment costs register pressure that Q4_K does not pay -- and here that outweighs the halved B traffic. **So activation traffic is not the only term, and NFRAG is not a free knob per type.** Q4_K keeps two fragments; Q6_K and Q5_K stay at one. Reverted; the branch is back to the 110.1 ms state. |
| 068 | 2026-08-12 | 066's headroom claim checked against the roofline — **it stands** | is the width 1->8 gap waste, or the irreducible cost of 7 more columns? | arithmetic from measured HARDWARE.md constants | — | width-8 column compute **24.8 ms** vs weight-stream floor **61.0 ms** | T_ver(8) is **1.81x** its floor | — | **066 confirmed, target real** | Before spending further on the kernel I checked whether 066 was comparing incomparable things -- T_ver(1) does one column, T_ver(8) does eight, so the 39 ms gap could simply be what seven extra columns cost. It is not. At width 8 the column arithmetic is 2 x 27.32e9 x 8 / 17.6 TFLOP/s = **24.8 ms**, comfortably inside the **61.0 ms** the weight stream needs at 270.8 GB/s, so with overlap the extra columns should be nearly free and T_ver(8) should approach T_ver(1). Measured 110.1 ms is **1.81x** that floor. The excess is imperfect overlap plus activation-read traffic, not column cost. **The ladder, at today's drafting (57.5 ms) and acceptance (3.738):** T_ver(8) 95 -> 24.51 tok/s, 85 -> 26.23, 71.1 -> **29.07**. So the kernel alone can carry this to ~29 and 30 needs a little from drafting as well -- but the verify side is where the mass is, and 067 only ruled out one mechanism (NFRAG per type), not the target. |
| 069 | 2026-08-12 | threadgroup-staged activations — **REJECT, -27%** | cooperative staging of the 8 KiB activation slice per super-block, shared by all NSG simdgroups | `llama-bench -p 5,8` under lock | device reads: pp8 **72.64**, T_ver(8) 110.1 ms | staged: pp8 **52.80**, T_ver(8) **151.5 ms** | **-27%** | — | **REJECT, reverted** | 068's top-ranked mechanism, and the reasoning was sound as far as it went: all NSG simdgroups iterate the same k-range and differ only in rows, so they read **identical** activations, and one cooperative load should serve four. Correctness passes. It is 27% slower. **The redundancy is real; the cost was not.** The activation slice is 8 KiB per super-block and 164 KiB in total, so it was already resident in cache and the four "redundant" reads were L1 hits, not traffic. What staging adds is two `threadgroup_barrier`s per super-block -- 40 per row-block -- which serialise four simdgroups that were previously independent, plus the staging loop itself. **This also refutes the shape of 066's diagnosis:** the 150 vs 232 GB/s gap is not activation *traffic* in the sense of bytes moved, since removing three quarters of those reads made things worse. Whatever costs the 39 ms is either instruction issue or overlap, not redundant loads. Worth noting the asymmetry with `mul_mm`: staging **weights** there is unavoidable and amortised over a wide tile; staging **activations** here buys nothing because they already fit in cache. |
| 070 | 2026-08-12 | manual software pipelining of the weight loads — **REJECT, -19%** | prefetch next group's `qs` before consuming the current one | `llama-bench -p 5,8` under lock | pp8 **72.64**, T_ver(8) 110.1 ms | pp8 **59.15**, T_ver(8) **135.3 ms** | **-19%** | — | **REJECT, reverted** | 069 concluded the 39 ms gap is overlap rather than bytes, so this targeted overlap directly: hoist each group's `qs` load one iteration ahead so its latency hides behind the dequant and matrix ops. Correctness passes; it is 19% slower. The Metal compiler was already scheduling those loads, and carrying `qvp`/`qv2p` live across the loop costs registers in a kernel that 067 already showed is register-sensitive. **Fourth consecutive mechanism to fail on this kernel** -- NFRAG per type (067), threadgroup-staged activations (069), F16 activations (dead by 069's argument), and now explicit pipelining. **The honest reading: this kernel is at its practical limit for its current structure.** It runs at 170 GB/s standalone against a 212.9 GB/s loads-only ceiling for its own access pattern -- 80% -- and every hand intervention since has moved it backwards. The 1.81x-above-roofline gap in 068 is real, but it is not reachable by tuning this design; it would take a different one. |
| 071 | 2026-08-13 | **GPU wired limit raised — the rig became measurable** | `iogpu.wired_limit_mb` 0 (18186 MiB default) -> 20480 | `bench/specab.sh --depth 7 --pairs 6` under lock | upstream **15.8874** | wired **23.4897** (steady-state ~22.8) | +47.9% vs upstream | **0.0312** | **KEEP** | Blocked since setup for want of sudo; now applied. The throughput change is modest (22.3 -> ~22.8 steady-state) but **the variance change is not: paired-delta sd falls 6.795 -> 0.693, a 10x collapse**, with CI [-33.09%, -31.64%] and swap at 0-28 pages against 222-13000 before. 064 diagnosed the spread as swap-driven and this confirms it directly. The rig now measures a *level*, not only a delta, which is the precondition for ever claiming a target number honestly. Not persisted anywhere: the credential was used once, in memory. Resets on reboot -- re-apply before any session that will quote absolute numbers. |
| 072 | 2026-08-13 | **40 tok/s recomputed from the identity — it is acceptance-bound** | `tok/s = mean_acc_per_fwd / (T_ver + drafting)` with today's measured terms | arithmetic on 062/071 measurements | — | — | — | — | **direction set** | At acc/fwd **3.738**, 40 tok/s needs a **93.5 ms** cycle -- but T_ver(8) alone is **110.1 ms**, so 40 is unreachable at today's acceptance *even with drafting free and verify untouched*. The requirement is acc/fwd **5.0** (cycle 125 ms, drafting 15 ms) or **6.0** (cycle 150 ms, drafting 40 ms), against 3.738 now: a **34-60% rise in accepted tokens per forward**. Verify work cannot supply it -- 067-070 closed four mechanisms there and the whole remaining verify headroom (T_ver 110 -> 71) is worth ~29 tok/s at *unchanged* acceptance. **So the direction for 40 is acceptance, and the ceiling is structural: a chain draft of 4 caps acc/fwd at 5, and 40 needs essentially all of it.** Width 8 admits 8. The mechanism that fills width with candidates rather than a single chain is tree drafting, which requires a tree attention mask in verification -- the one large piece of machinery this effort has repeatedly identified and never built. |

| 073 | 2026-08-13 | **the verify staircase past width 8 — a 3x cliff nobody had measured** | `llama-bench -p 1..64 -n 0` on the current stack | `bench/lock.sh`, quiet machine, throttle 0.1% | W=8 **115.1 ms** | W=9 **328.3**, W=10 334.1, W=16 338.2, W=32 347.9, W=64 377.2 | +213 ms as a **fixed step at W=9**, flat thereafter | — | **measured, redirects tree drafting** | Every width measurement in this project stopped at 8, because that is where the sgmv gate stops. Past it the cost triples and stays tripled: T_ver goes 115 -> 328 between W=8 and W=9 and then grows only 49 ms out to W=64. **It is a path switch, not a per-token cost.** With `GGML_METAL_SGMV_DISABLE=1` the same step appears (W=8 215.2 -> W=9 328.3), so it is not our kernel: at ne11=9 the K-quant path falls off `mul_mv_ext` (gated ne11 4..8) into `mul_mm`. Also visible and unexplained: **W=3 costs 134.9 ms, worse than W=4's 113.3**, the gap `c1-kquant-ext-gate` was opened for and never closed. **This is the single most important measurement for tree drafting**, which needs width 16-32 and would until now have paid 3x for it. |
| 074 | 2026-08-13 | **`simdgroup_float8x8` is NOT the matrix units — 035's founding assumption is false** | peak MMA throughput with zero memory traffic | `probe/mmapeak.m`, register-resident, iteration sweep linear | scalar `fma` f32 **5.6-5.7 TFLOP/s** | `simdgroup_float8x8` **6.07-6.15**, `simdgroup_half8x8` **6.04-6.18** | MMA = scalar rate, **half gives nothing** | n/a | **CORRECTS 035, 055, 068** | LEDGER 035 asserted "the matrix units run at 17.6 TFLOP/s and are the only thing that clears [the per-column bound]" and the entire register-resident sgmv kernel was built on it. The number was never measured; it was `mul_mm`'s n=512 rate, which comes from the **tensor API** (MetalPerformancePrimitives `matmul2d`) — a different unit on a different code path. `simdgroup_multiply_accumulate` is decomposed onto the ordinary FP32 ALUs and peaks at **6.1 TFLOP/s**, confirmed against a scalar-fma control at 5.6 in the same harness. Three consequences. (a) **sgmv at n=8 reaches 4.10 TFLOP/s = 67% of its own real ceiling**, so it was near its structural limit all along — which is the missing explanation for 055, 067, 069 and 070 all failing on it, and it means a fifth tweak was never going to work. (b) Any simdgroup/scalar kernel costs **8.96 ms per verify column**, so T_ver(8) >= 71.7, **T_ver(16) >= 143, T_ver(32) >= 287 — wide tree drafting is arithmetically dead on this path.** (c) 055's "half fragments should be 2x" premise is dead at the source: fp16 MMA is the same rate as fp32 on this part. |
| 075 | 2026-08-13 | the tensor API is the only path with headroom, and Q4_K already reaches it | `test-backend-ops perf -o MUL_MAT` on m=4096 k=14336 | perf sweep under lock | sgmv at n=8: 229.1 us = **4.10 TFLOP/s** = 1.88x the 122 us bandwidth floor | tensor `mul_mm` Q4_K at n=512: 3664.8 us = **16.41 TFLOP/s** (f16 16.55, q6_K 15.91) | **4.0x headroom in arithmetic rate** | — | **sets the direction** | The tensor units are real and they dequantize K-quants at full rate — Q4_K reaches 16.41 TFLOP/s, within 1% of f16's 16.55, so quantization is not what holds them back. At **3.33 ms per verify column** instead of 8.96, the floors invert: T_ver(8) floor becomes **61 ms (bandwidth)** rather than 71.7 (compute), and **T_ver(16) floor is still 61 ms** — width 16 becomes free, which is exactly what tree drafting needs. The obstacle is tile shape, not the units: the tensor `mul_mm` tile is NRA=64 x **NRB=128**, so at ne11=9 it issues 2*27.32e9*128 = 6.99 TFLOP for 9 columns, which at 16.4 TFLOP/s is 426 ms and explains 073's measured 328 directly. **The N tile is the whole cliff.** |
| 076 | 2026-08-13 | **narrow-N tensor tile: 034 re-tested against the real stack, and it wins where the cliff is** | tensor `mul_mm` at N tiles of 8/16/32 for K-quants vs upstream routing | `llama-bench -p 4..32 -n 0` under lock, one binary, `GGML_METAL_NO_NARROW_N` toggle | upstream: pp8 **111.3 ms**, pp16 333.3, pp32 342.5 | narrow-N: pp8 168.9, pp16 **188.1**, pp32 **214.2** | **1.8x at ne11 9-32**, 1.5x loss at 4-8 | test-backend-ops q4_K 3/3, n8/n16/n32 pipelines confirmed by name | **KEEP, gated ne11 >= 9** | Predicted 108/134/188 for widths 8/16/32 from a serialized staging model; measured 169/188/214, so the slope was right and the fixed cost was underestimated by ~2x. Both paths fit clean lines over 4..32: **sgmv = 65.7 + 5.70*W**, **narrow-N = 154 + 1.89*W**. The tensor units cut the per-column cost 3x (5.70 -> 1.89, against 3.33 theoretical at 16.4 TFLOP/s) — that part of 075 is confirmed. What kills it at small W is a **154 ms fixed cost**, and that is 034's effect arriving by a different mechanism than 034 claimed: only A is staged and B is read straight from device memory, so for any ne11 <= NRB there is exactly **one** N tile and the total A-staging is identical whatever the tile width. Narrowing does not stage more; it leaves too little matmul work to hide the staging the 128-wide tile hides completely. They cross at W ~ 23. **Gated to ne11 >= 9**, where it replaces a flat 330+ ms path with 180-215 and cannot touch the production width. Two defects in the original also fixed: the `_n16` instantiation was built with 32, and `res.nr1` stayed 128 on the tensor path while a narrow kernel ran. |
| 077 | 2026-08-13 | **what 40 actually requires, recomputed from floors rather than from today's kernels** | `tok/s = acc_per_fwd / (T_ver + D*t_draft)` with every term at its measured floor | arithmetic over 073-076 | today: T_ver(8) **111** + drafting **46** = 157 ms at acc/fwd 3.738 -> 23.8 | floors: T_ver(8) **61** (16.52 GB / 270.8 GB/s), t_draft **4.8** (994 MiB head + MTP block), D=4 -> cycle **80 ms** | **46.7 tok/s at TODAY'S acceptance** | — | **direction, and it contradicts 072** | LEDGER 072 declared 40 acceptance-bound and put tree drafting on the critical path. That conclusion held T_ver(8)=110 fixed, which is the failure mode GOAL names explicitly. At the floors the same identity gives **46.7 tok/s with acc/fwd unchanged at 3.738**, so 40 is reachable without any acceptance gain at all — the entire gap is implementation slack: T_ver(8) is **1.82x** its bandwidth floor and t_draft is **2.4x** its own. 40 tok/s needs cycle 93.5 ms at today's acceptance, e.g. T_ver 75 + drafting 19. **Both terms still have to move, as GOAL insists — but they are cost terms, not acceptance.** Ranked by size of the gap to floor: drafting is 46 ms of a 157 ms cycle and has had almost no work; verify is 50 ms above floor and has had four failed mechanisms, all of which 074 now explains. Acceptance moves from 3.738 to ~4.2 only as the last step, and 40 no longer depends on it. |
| 078 | 2026-08-13 | staging barriers are not the tensor path's fixed cost — the dequant is | `N_MM_NK` 2 -> 8, i.e. K-chunk 32 -> 128, cutting threadgroup barriers 4x | `llama-bench -p 4..32 -n 0` under lock, both arms | narrow-N at K-chunk 32: pp10 179.7, pp16 **188.1**, pp32 214.2 | K-chunk 128: pp10 171.9, pp16 **179.1**, pp32 204.0 | **only -5%**; 128-wide path got **13% worse** (pp10 329 -> 371) | test-backend-ops q4_K 3/3 | **REJECT, constant reverted** | Predicted the intercept would fall 154 -> ~105 and T_ver(16) to ~135. It fell to ~157/179. **160 K-iterations x 2 barriers per threadgroup is not what the 154 ms is made of.** Backing it out: the fixed cost is ~95 ms above the 61 ms read floor for 33e9 weights = **~8.3 ALU ops per weight** to dequantize Q4_K and store it into threadgroup memory. That is the real quantity, and vectorizing the 16 scalar stores plus hoisting the bounds check could plausibly reach ~5 ops/weight — worth ~40 ms, leaving the intercept near 118 and T_ver(8) near 130. **Still above sgmv's 110, so this closes the tensor path at width 8**: for K-quants at narrow widths, dequantizing into a memory-backed tensor costs more than the arithmetic the tensor units save. It stays open and worth 1.8x from ne11 9 up. Larger K-chunks also cost the 128-wide tile 13% (less occupancy per threadgroup), so the constant is reverted rather than traded. |
| 079 | 2026-08-13 | **tree drafting at width 8 is FREE, and that reframes the whole endgame** | re-reading the sgmv staircase per-width instead of as a slope | 076's arm A, widths 4/5/6/8 | 109.2 / 107.9 / 111.1 / **111.3 ms** | the kernel is **flat**: it computes 8 columns whatever ne11 is, and masks the output | marginal cost of columns 5-8 is **~0.4 ms/col** | — | **sets the endgame** | LEDGER 072 put tree drafting on the critical path but assumed it needed width 16-32 and therefore an expensive new verify. **It does not.** `kernel_mul_mv_sgq4k_f32` takes `nc = args.ne11` only to mask which columns it *stores*; the arithmetic is a fixed 8-wide simdgroup fragment. So verifying 8 tree candidates costs exactly what verifying today's 4-token chain costs, on the kernel already merged. My earlier fit of "65.7 + 5.70*W" was an artifact of including the width-1 point, which is `mul_mv`, a different kernel — the flat reading is the right one. **Consequence: the 8 verify slots are already paid for and today's configuration wastes them**, filling them with a 4-step MTP chain plus a 3-token union extension for acc/fwd 3.738 aggregate (4.80 measured on codeedit-01, 3.05 on prose-01). At cycle 137 ms, 40 tok/s needs acc/fwd **5.48**; with drafting cut to D=3 at its 4.8 ms floor, cycle 125 ms needs **5.0**, which is exactly the cap of a 4-chain and the natural range of an 8-node tree. **Width 16 is the trap**: narrow-N puts T_ver(16) at 179 ms, so a 16-node tree would need acc/fwd 8.2. The target configuration is 8 slots, tree-structured, depth 3-4. |
| 080 | 2026-08-13 | **tree drafting at width 8 is FALSIFIED — by the per-position acceptance the server already logs** | `-lv 5` exposes `acc per pos`, the cumulative P(position i accepted), per category | 6 frozen prompts, one server lifetime, temp 0 | prose-01 **.792 .623 .396 .340 \| .075 .057 .038**; json-01 **.970 .970 .879 .758 \| .485 .364 .303** | positions 1-4 are the MTP chain, 5-7 the n-gram extension | swapping 3 ext slots for 3 tree siblings: prose **+2.2%**, longctx +5.1%, codeedit **-11.7%**, json **-19.3%** | — | **REJECT — closes the line 052/072 pointed at** | 079 established the 8 verify slots are free, so the natural next move was to fill them with a tree. The data says do not. **Trees pay when slots are idle; here 7 of 8 are already earning.** On structured content the draft's top-1 is right 97% of the time, so a sibling is worth 0.011-0.036 tokens while the ext token it would displace is worth 0.139-0.303 — hedging a draft that is already right is nearly pure waste. Only on prose and longctx, where top-1 acceptance is 0.79/0.92 and decays fast, is a sibling worth more than an ext token (0.083-0.154 vs 0.038-0.077), and even there the swap is **+2.2%**, under the 3.0% MDE. So the mechanism this effort has named as the missing piece since 052 — and 072 put on the critical path — is worth roughly nothing at the only width that is affordable, and is negative in aggregate. It was falsified in half an hour from a counter the server was already keeping, against weeks of tree attention plus an SSM parent-index scan. **Trees are not the way to 40. The extension already does the job trees would do, better, wherever the draft is weak enough for it to matter.** |
| 081 | 2026-08-13 | the cycle is constant across categories, so tok/s IS acceptance | per-prompt cycle from server timings at the frozen config | 6 categories, 192 tokens each | cycle **138.8, 139.7, 143.9, 145.2, 144.1, 141.8 ms** | spread **4.6%** while tok/s spans **16.7 - 37.0** | `tok/s = acc/fwd x 7.1` to within noise | — | **frames what is left** | Every category costs the same 140 ms per forward pass and differs only in how many tokens that pass buys: longctx 2.37, prose 2.70-2.78, codegen 4.17, codeedit 4.80, json 5.33. **json-01 already runs at 37.01 tok/s**, within 8% of target, purely on acceptance. So there are exactly two dials and their product must hit 40: **cycle 140 -> 93 ms at today's aggregate acceptance**, or acceptance 3.7 -> 5.6 at today's cycle. 080 closes the acceptance dial at this width. On cost, T_ver(8)=110 decomposes as ~72 ms of MMA (8 columns x 27.32e9 x 2 at the 6.1 TFLOP/s of 074) plus ~23 ms of dequant ALU, with the 61 ms weight stream hiding underneath — i.e. **sgmv is ALU-throughput-bound, not load-bound**, which is the fourth independent confirmation of why 069 and 070 failed. Floor ~72-80 ms. With drafting at its 22 ms floor the best cycle is ~102 ms = **36.3 tok/s aggregate**, with json above 50. Reaching 40 needs the dequant ALU cut AND a draft improvement that is not a tree. |
